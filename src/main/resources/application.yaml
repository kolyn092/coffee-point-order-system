spring:
  application:
    name: coffee-point-order-system
  datasource:
    url: "jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:coffee_point_order}"
    username: ${DB_USERNAME:coffee}
    password: ${DB_PASSWORD:coffee}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-init-sql: "SET time_zone = '+00:00'"
      data-source-properties:
        characterEncoding: UTF-8
        connectionTimeZone: UTC
        forceConnectionTimeZoneToSession: true
        useUnicode: true
  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
    admin:
      auto-create: true
    consumer:
      group-id: ${POPULAR_MENU_CONSUMER_GROUP_ID:popular-menu}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        "[spring.json.trusted.packages]": com.coffeepointordersystem.domain.order.event
        "[spring.json.value.default.type]": com.coffeepointordersystem.domain.order.event.OrderCompletedEvent
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JacksonJsonSerializer
      properties:
        max.block.ms: ${OUTBOX_RETRY_PUBLISH_TIMEOUT_MILLIS:5000}
  jackson:
    time-zone: UTC

popular-menu:
  consumer:
    concurrency: ${POPULAR_MENU_CONSUMER_CONCURRENCY:1}
    enabled: ${POPULAR_MENU_CONSUMER_ENABLED:true}

outbox:
  retry:
    fixed-delay: ${OUTBOX_RETRY_FIXED_DELAY:1s}
    publish-timeout-millis: ${OUTBOX_RETRY_PUBLISH_TIMEOUT_MILLIS:5000}
